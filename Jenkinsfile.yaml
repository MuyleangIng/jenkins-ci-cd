pipeline:
  agent:
    label: 'linux'
  stages:
    - stage: 'Build'
      steps:
        - script: |
            echo 'Building...'
            # Add your build commands here
    - stage: 'Test'
      steps:
        - script: |
            echo 'Testing...'
            # Add your test commands here
    - stage: 'Deploy'
      steps:
        - script: |
            echo 'Deploying...'
            # Add your deploy commands here
